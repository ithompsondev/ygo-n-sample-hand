<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Decklist | <%= name %></title>
</head>
<body>
    <h2><%= name %> Decklist</h2>
    <div style="display:grid;grid-template-columns:1fr 1fr 1fr 1fr 1fr;grid-gap:20px;">
        <% for (let i = 0; i < deck.length; i++) { %>
            <img src="<%= deck[i]['art_small'] %>" alt="<%= deck[i]['name'] %>">
        <% } %>
    </div>
</body>
</html>